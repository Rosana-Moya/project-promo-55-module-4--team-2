const mysql = require("../database/mysql-pool");

const postProject = async (req, res) => {
    try {

    } catch {
        res.send("Algo ha ido mal");
    }
};


module.exports = {
    postProject
};